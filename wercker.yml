box:
    id: $DOCKER_USERNAME/miksu
    tag: 1.0
    registry: https://registry.hub.docker.com

build:
  steps:
    - npm-install
deploy:
  steps:
    - internal/docker-push:
        username: $DOCKER_USERNAME
        password: $DOCKER_PASSWORD
        tag: $WERCKER_MAIN_PIPELINE_STARTED
        repository: $DOCKER_USERNAME/miksu
        ports: "3000"
        cmd: npm start
        registry: https://registry.hub.docker.com
    - script: 
        name: redeploy-OCCS
        code: |
              curl -k -d "token=rdljntlc" -X POST https://140.86.0.102/api/v2/deployments/Miksu-20170919-151940/webhook/restart
